services:
  obs-control:
    build: .
    container_name: obs-control
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      PANEL_JSON_PATH: "/app/panel.json"
